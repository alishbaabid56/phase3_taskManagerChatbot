// postcss.config.mjs
export default {
  plugins: {
    '@tailwindcss/postcss': {},
    // Optional: agar autoprefixer use kar rahe ho to add kar do (recommended for better browser support)
    // autoprefixer: {},
  },
};